import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as GridStories from './Grid.stories';

<Meta of={GridStories} />

# Grid

CSS Grid layout primitive. Two modes: fixed column count or responsive `auto-fit` based on a minimum item width.

---

## When to use

- Dashboard stat cards or metric tiles that should reflow as the viewport narrows.
- Equal-width column layouts (form rows, image galleries).
- Any scenario where Stack or Inline don't give you 2D control.

---

## Playground

<Canvas of={GridStories.Playground} />
<Controls of={GridStories.Playground} />

---

## All states

<Canvas of={GridStories.AllStates} />

---

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>as</code></td>
      <td><code>React.ElementType</code></td>
      <td><code>'div'</code></td>
      <td>HTML element to render</td>
    </tr>
    <tr>
      <td><code>columns</code></td>
      <td><code>number</code></td>
      <td>—</td>
      <td>Fixed column count. Mutually exclusive with <code>minItemWidth</code>.</td>
    </tr>
    <tr>
      <td><code>minItemWidth</code></td>
      <td><code>number | string</code></td>
      <td>—</td>
      <td>Minimum item width. Numbers are treated as px; strings are used as-is (e.g. <code>'50%'</code>). Grid uses <code>auto-fit</code> + <code>minmax</code>.</td>
    </tr>
    <tr>
      <td><code>gap</code></td>
      <td><code>CreSpace</code></td>
      <td><code>'micro'</code></td>
      <td>Gap between cells — references <code>--cre-space-*</code> tokens</td>
    </tr>
    <tr>
      <td><code>align</code></td>
      <td><code>React.CSSProperties['alignItems']</code></td>
      <td>—</td>
      <td>Cross-axis alignment per row</td>
    </tr>
    <tr>
      <td><code>justify</code></td>
      <td><code>React.CSSProperties['justifyItems']</code></td>
      <td>—</td>
      <td>Item alignment within each cell</td>
    </tr>
    <tr>
      <td><code>className</code></td>
      <td><code>string</code></td>
      <td>—</td>
      <td>Additional CSS class</td>
    </tr>
    <tr>
      <td><code>style</code></td>
      <td><code>React.CSSProperties</code></td>
      <td>—</td>
      <td>Inline styles</td>
    </tr>
    <tr>
      <td><code>children</code></td>
      <td><code>React.ReactNode</code></td>
      <td>—</td>
      <td>Grid cells</td>
    </tr>
  </tbody>
</table>

---

## Responsive vs fixed

Use `minItemWidth` when you want items to collapse from multi-column to single-column automatically:

```tsx
<Grid minItemWidth={240} gap="micro">
  {items.map(item => <StatCard key={item.id} {...item} />)}
</Grid>
```

Use `columns` when you need a strict, fixed layout:

```tsx
<Grid columns={3} gap="nano">
  <FormField /><FormField /><FormField />
</Grid>
```
