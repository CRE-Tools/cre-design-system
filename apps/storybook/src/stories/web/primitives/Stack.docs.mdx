import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as StackStories from './Stack.stories';

<Meta of={StackStories} />

# Stack

Vertical flex layout with a token-driven gap between items. The default building block for stacking content sections, form fields, and card bodies.

---

## When to use

- Stacking a series of elements top-to-bottom with consistent spacing.
- Composing a form (Field components flow naturally in a Stack).
- Laying out content inside a Card, Surface, or Drawer.
- Use `Inline` for horizontal arrangements; `Grid` for grid layouts.

---

## Playground

<Canvas of={StackStories.Playground} />
<Controls of={StackStories.Playground} />

---

## All states

<Canvas of={StackStories.AllStates} />

---

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>as</code></td>
      <td><code>React.ElementType</code></td>
      <td><code>'div'</code></td>
      <td>HTML element to render</td>
    </tr>
    <tr>
      <td><code>gap</code></td>
      <td><code>CreSpace</code></td>
      <td><code>'micro'</code></td>
      <td>Spacing between items — references <code>--cre-space-*</code> tokens</td>
    </tr>
    <tr>
      <td><code>align</code></td>
      <td><code>React.CSSProperties['alignItems']</code></td>
      <td>—</td>
      <td>Cross-axis alignment</td>
    </tr>
    <tr>
      <td><code>justify</code></td>
      <td><code>React.CSSProperties['justifyContent']</code></td>
      <td>—</td>
      <td>Main-axis alignment</td>
    </tr>
    <tr>
      <td><code>className</code></td>
      <td><code>string</code></td>
      <td>—</td>
      <td>Additional CSS class</td>
    </tr>
    <tr>
      <td><code>style</code></td>
      <td><code>React.CSSProperties</code></td>
      <td>—</td>
      <td>Inline styles</td>
    </tr>
    <tr>
      <td><code>children</code></td>
      <td><code>React.ReactNode</code></td>
      <td>—</td>
      <td>Content</td>
    </tr>
  </tbody>
</table>

---

## Spacing scale

`gap` maps directly to the `--cre-space-*` token family. Commonly used values:

<table>
  <thead>
    <tr>
      <th>Token</th>
      <th>Use</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>quark</code></td>
      <td>Tightest — label + input, badge + text</td>
    </tr>
    <tr>
      <td><code>nano</code></td>
      <td>Within a group — inline items, list rows</td>
    </tr>
    <tr>
      <td><code>micro</code></td>
      <td>Between groups — card sections, form fields</td>
    </tr>
    <tr>
      <td><code>small</code></td>
      <td>Page sections</td>
    </tr>
    <tr>
      <td><code>large</code></td>
      <td>Top-level layout gaps</td>
    </tr>
  </tbody>
</table>
