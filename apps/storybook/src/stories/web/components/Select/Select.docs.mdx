import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as SelectStories from './Select.stories';

<Meta of={SelectStories} />

# Select

A custom single-select dropdown with keyboard navigation. Replaces the native `<select>` with a token-styled dropdown that matches the Input appearance. Always pair with `Field` for a label and error state.

---

## When to use

- Choosing one option from a small-to-medium list (2–20 items).
- Filters, status pickers, category selectors.
- For boolean choices use `Checkbox`; for large lists consider search-driven alternatives.

---

## Basic

<Canvas of={SelectStories.Basic} />

---

## Disabled

<Canvas of={SelectStories.Disabled} />

---

## Props

Use the controls below to tweak props and see how the component responds.

<Canvas of={SelectStories.Playground} />

<Controls of={SelectStories.Playground} />

---

## Keyboard behaviour

<table>
  <thead>
    <tr>
      <th>Key</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>Enter</code> / <code>Space</code></td>
      <td>Open the dropdown</td>
    </tr>
    <tr>
      <td><code>ArrowDown</code> / <code>ArrowUp</code></td>
      <td>Navigate options</td>
    </tr>
    <tr>
      <td><code>Enter</code></td>
      <td>Select highlighted option</td>
    </tr>
    <tr>
      <td><code>Escape</code></td>
      <td>Close without selecting</td>
    </tr>
  </tbody>
</table>

---

## With Field

```tsx
<Field label="Status" htmlFor="status-select">
  <Select
    id="status-select"
    value={status}
    onChange={setStatus}
    placeholder="Pick a status…"
    options={[
      { value: 'active', label: 'Active' },
      { value: 'archived', label: 'Archived' },
    ]}
  />
</Field>
```
