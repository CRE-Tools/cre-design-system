import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

# Button

Buttons are used to trigger user actions across CRE applications.

Common usage patterns:

- Primary actions (e.g. "Confirm", "Continue", "Save")
- Secondary actions (e.g. "Cancel", "Back")
- Inline actions inside panels (e.g. "Edit", "Remove")

All visual values come from CSS custom properties set by `CreThemeProvider`. The button never references raw hex values — it is fully theme-driven and supports light and dark mode out of the box.

---

## Playground

Use the controls below to tweak props and see how the component responds.

<Canvas of={ButtonStories.Playground} />

<Controls of={ButtonStories.Playground} />

---

## Size variants

Three sizes are available via the `size` prop:

<table>
  <thead>
    <tr>
      <th>Size</th>
      <th>Use case</th>
      <th>Font</th>
      <th>Height</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>regular</code></td>
      <td>Standard web UI</td>
      <td>15px / 500</td>
      <td>40px fixed</td>
    </tr>
    <tr>
      <td><code>large</code></td>
      <td>Desktop primary actions</td>
      <td>16px / 500</td>
      <td>auto (12px vertical padding)</td>
    </tr>
    <tr>
      <td><code>vr</code></td>
      <td>VR / spatial UI, extra-large touch targets</td>
      <td>24px / 400</td>
      <td>auto (16px vertical padding)</td>
    </tr>
  </tbody>
</table>

<Canvas of={ButtonStories.SizeComparison} />

---

## Icon variants

Use `leadingIcon` and `trailingIcon` to render icons before and after the label. Icons are sized and padded via token vars and inherit the button's foreground color.

<Canvas of={ButtonStories.WithLeadingIcon} />

<Canvas of={ButtonStories.WithTrailingIcon} />

<Canvas of={ButtonStories.WithBothIcons} />

---

## Disabled state

Set `disabled` to block all interaction and apply disabled token colors.

<Canvas of={ButtonStories.Disabled} />

---

## All states

Quick visual QA across all icon configurations.

<Canvas of={ButtonStories.AllStates} />

---

## Icon alignment

Verifies icon slot dimensions, gap, and padding across all three sizes.

<Canvas of={ButtonStories.IconAlignment} />
