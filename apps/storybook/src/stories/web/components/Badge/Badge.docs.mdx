import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as BadgeStories from './Badge.stories';

<Meta of={BadgeStories} />

# Badge

A small inline status label. Badges communicate the category or state of an item — they never trigger actions.

---

## When to use

- Displaying the status of a record in a table row (active, new, archived).
- Tagging an item with a category or label.
- Showing notification counts or feature flags.
- Do **not** use Badge as a button or clickable element.

---

## Variants

Five semantic variants map to the token system's feedback and accent palettes.

<Canvas of={BadgeStories.AllStates} />

---

## Usage in context

<Canvas of={BadgeStories.WithLabels} />

---

## Props

Use the controls below to tweak props and see how the component responds.

<Canvas of={BadgeStories.Playground} />

<Controls of={BadgeStories.Playground} />

---

## Variant guide

<table>
  <thead>
    <tr>
      <th>Variant</th>
      <th>CSS tokens</th>
      <th>Use case</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>neutral</code></td>
      <td><code>--cre-color-bg</code> / <code>--cre-color-text-muted</code></td>
      <td>Draft, inactive, default state</td>
    </tr>
    <tr>
      <td><code>success</code></td>
      <td><code>--cre-feedback-success-*</code></td>
      <td>Live, published, healthy</td>
    </tr>
    <tr>
      <td><code>warning</code></td>
      <td><code>--cre-feedback-alert-*</code></td>
      <td>Review needed, degraded</td>
    </tr>
    <tr>
      <td><code>error</code></td>
      <td><code>--cre-feedback-error-*</code></td>
      <td>Failed, blocked, critical</td>
    </tr>
    <tr>
      <td><code>accent</code></td>
      <td><code>--cre-accent-*</code></td>
      <td>Selected, highlighted, new</td>
    </tr>
  </tbody>
</table>
